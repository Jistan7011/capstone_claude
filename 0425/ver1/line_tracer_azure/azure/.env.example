# Azure VM 중계 서버 환경 변수
# cp .env.example .env 후 SECRET_KEY 값을 변경하세요.

# 내부 서버 포트 (Docker로 실행 시 외부 80 → 내부 8000 매핑)
PORT=8000

# Flask 세션 보안 키 — 반드시 랜덤 문자열로 변경
SECRET_KEY=change-this-to-a-random-secret
